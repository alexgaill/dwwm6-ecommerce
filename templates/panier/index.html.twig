{% extends 'base.html.twig' %}

{% block body %}
	<div class="mt-5">
		<table>
			<thead>
				<tr>
					<th>{{'panier.name'|trans}}</th>
					<th>{{'panier.quantity'|trans}}</th>
					<th>{{'panier.total'|trans}}</th>
				</tr>
			</thead>
			<tbody>
                {% for panier in paniers %}
                    <tr>
                        <td>{{panier.produit.nom}}</td>
                        <td>{{panier.quantite}}</td>
                        <td>{{panier.total}}</td>
                    </tr>
                {% else %}
                      <tr>
                        <td colspan="3">{{'panier.no'|trans}}</td>
                      </tr>  
                {% endfor %}
            </tbody>
            <tfoot>
                <tr>
                    <td colspan=2 class="text-end">Total TTC</td>
                    <td>{{totalTTC}}</td>
                </tr>
                <tr>
                    <td colspan="3" class="text-center"><a href={{ path('payer') }}><button class="btn btn-primary">Payer</button></a></td>
                </tr>
            </tfoot>
		</table>
	</div>
{% endblock %}
