{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{'product.edit'|trans}}</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('produit_index') }}">{{'back.list'|trans}}</a>

    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
